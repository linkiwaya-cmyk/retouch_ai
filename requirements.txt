--extra-index-url https://download.pytorch.org/whl/cu121
torch
torchvision

fastapi
uvicorn[standard]
python-multipart

opencv-python-headless
opencv-contrib-python-headless  # для GuidedFilter (ximgproc)
numpy
Pillow>=10.0.0

# ── Ключевая зависимость — MediaPipe ──────────────────────────────────────────
mediapipe>=0.10.0              # Face Mesh + Selfie Segmentation

# ── iPhone / HEIC ─────────────────────────────────────────────────────────────
pillow-heif>=0.16.0
piexif

# ── Face detection (fallback) ─────────────────────────────────────────────────
insightface
facexlib
onnxruntime-gpu                # замени на onnxruntime если нет GPU

# ── Bot ───────────────────────────────────────────────────────────────────────
aiogram>=3.7.0
aiohttp
python-dotenv

# ── Опционально ───────────────────────────────────────────────────────────────
# gfpgan                       # GFPGAN face retouch (если захочешь добавить)
# scikit-image                 # дополнительные алгоритмы
