--extra-index-url https://download.pytorch.org/whl/cpu
torch

fastapi
uvicorn[standard]
python-multipart
opencv-python-headless
numpy
Pillow>=10.0.0

# ── iPhone (HEIC/HEIF) ─────────────────────────────────────────────────────────
pillow-heif>=0.16.0        # HEIC/HEIF decode (основной)
pyheif                      # fallback для старых HEIC файлов

# ── EXIF / метаданные ─────────────────────────────────────────────────────────
piexif                      # чтение/запись EXIF (ориентация)
exifread                    # дополнительный парсер EXIF

# ── Android (WebP, JPEG, PNG) ─────────────────────────────────────────────────
# Pillow>=10 уже поддерживает WebP, JPEG, PNG нативно
# opencv-python-headless поддерживает все основные форматы Android

# ── Face detection & parsing ──────────────────────────────────────────────────
insightface
facexlib
onnxruntime-gpu             # GPU (если нет GPU — заменить на onnxruntime)

# ── AI retouch ────────────────────────────────────────────────────────────────
codeformer-pip

# ── Bot ───────────────────────────────────────────────────────────────────────
aiogram>=3.7.0
aiohttp
python-dotenv
